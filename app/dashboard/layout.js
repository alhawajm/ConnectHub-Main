// Layout wrapper for dashboard routes
// The actual auth check and data fetching happens inside each dashboard page
export default function DashboardRootLayout({ children }) {
  return <>{children}</>
}
